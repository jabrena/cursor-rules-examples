# Application Configuration — UTF-8 encoded
spring.application.name=problem5

# Server configuration
server.port=8080

# Logging configuration
logging.level.info.jab.latency=DEBUG
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
